# Robust Fix for PgBouncer Permissions

I will resolve the `postgresql/ha` issue by implementing a robust initialization strategy using an `init` container. This avoids permission issues with host-mounted files.

## 1. Create `pgbouncer-init.sh`
I will create a script `postgresql/ha/pgbouncer-init.sh` that:
-   Creates the necessary configuration files (`pgbouncer.ini`, `users.txt`) dynamically.
-   Sets the correct ownership (`chown 70:70` for `postgres` user inside the container).
-   This script will be mounted into an init container.

## 2. Update `docker-compose.yml`
I will modify `postgresql/ha/docker-compose.yml` to:
-   **Add Volume**: Create a shared volume `pgbouncer-config` between the init container and the main pgbouncer service.
-   **Add Init Service**: Use a lightweight image (e.g., `alpine`) to run the init script, populate the volume, and fix permissions.
-   **Update Main Service**:
    -   Remove `user: root`.
    -   Mount the `pgbouncer-config` volume to `/etc/pgbouncer`.
    -   Ensure it depends on the init service completing successfully.

## 3. Verify Fix
I will run `python3 run_tests.py` again. This should finally allow `postgresql/ha` to pass.
