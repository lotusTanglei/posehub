# Correct Redis Sentinel YAML and Update PostgreSQL Image

I will fix the YAML syntax error in `redis/sentinel` and use a more specific, widely-available tag for `bitnami/postgresql-repmgr` to ensure it pulls correctly.

## 1. Fix Redis Sentinel (`redis/sentinel/docker-compose.yml`)
-   **Issue**: The previous edit accidentally merged `volumes` and `healthcheck` lines, causing a syntax error.
-   **Action**: Correct the indentation and structure of the `redis-master` service definition.

## 2. Update PostgreSQL HA Image (`postgresql/ha/docker-compose.yml`)
-   **Issue**: `bitnami/postgresql-repmgr:16` manifest was not found.
-   **Action**: Switch to a highly stable, specific tag: `bitnami/postgresql-repmgr:16-debian-12`. This avoids ambiguity and ensures we get a valid image manifest.

## 3. Verify Fix
-   Run `python3 run_tests.py` again.
-   This time, both services should pass, completing the 100% success rate.
