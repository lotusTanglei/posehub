# Fix PostgreSQL HA Configuration

I will fix the issues causing the `pgbouncer` container to restart loop in `postgresql/ha`.

## 1. Update `pgbouncer.ini`
I will modify `posehub/postgresql/ha/pgbouncer.ini` to:
-   **Fix Hostname**: Change `host=postgres` to `host=postgres-primary` to match the service name in `docker-compose.yml`.
-   **Disable Logging to File**: Remove or comment out `logfile` and `pidfile` settings to let Pgbouncer log to stdout/stderr (Docker standard) and avoid permission issues with `/var/log/pgbouncer`.

## 2. Update `docker-compose.yml`
I will verify `posehub/postgresql/ha/docker-compose.yml`.
-   It currently mounts `./pgbouncer.ini` and `./users.txt`.
-   I will ensure the mount paths are correct (they seem fine).

## 3. Verify Fix
After applying the changes, I will run the runtime validation script specifically for this service:
`./validate_runtime.sh postgresql/ha`
