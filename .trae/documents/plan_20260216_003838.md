# Try Nacos v2.4.1 for ARM64

It seems `v2.3.2` also failed to pull on this specific environment, possibly due to a transient registry issue or a specific manifest mismatch on the node. I will try the latest stable version `v2.4.1`, which has confirmed multi-arch support.

## 1. Update `docker-compose.yml`
I will modify `nacos/standalone/docker-compose.yml` to use `image: nacos/nacos-server:v2.4.1`.

## 2. Verify Fix
I will run the test runner `python3 run_tests.py` again.
