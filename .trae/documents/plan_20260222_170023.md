# Final Fix for PostgreSQL HA and Verification

I will resolve the persistent Bitnami image issues by switching back to official, proven images and configurations that are guaranteed to work on your ARM64 environment.

## 1. Rewrite `postgresql/ha/docker-compose.yml`
I will use the official `postgres:16-alpine` image to create a simple Master-Slave replication setup.
-   **Primary**: Standard `postgres:16-alpine`.
-   **Replica**: Starts as a standby, replicates from primary.
-   **PgBouncer**: Use `edoburu/pgbouncer` (a very popular, lightweight alternative that handles config via env vars much better than the official one) or stick to the official one but with the correct env vars we discovered earlier. I will use the **official `pgbouncer/pgbouncer`** again but this time with the correct `DATABASES_HOST` env var which we identified as the fix in step 4 (before the Bitnami detour).

## 2. Verify
-   Run `python3 run_tests.py`.
-   This time, `postgresql/ha` will use the image we *know* works (`postgres:16-alpine`), and `redis/sentinel` is already fixed.
-   Expect **100% Pass**.
