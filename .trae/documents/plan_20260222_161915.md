# Fix PostgreSQL HA Permissions

I will resolve the recurring `pgbouncer` restart loop in `postgresql/ha` by addressing the file permission issue.

## 1. Update `docker-compose.yml`
I will modify `postgresql/ha/docker-compose.yml` to force the `pgbouncer` container to run as `root`.
-   **Action**: Add `user: root` to the `pgbouncer` service definition.
-   **Reason**: This bypasses the permission restrictions on the mounted `users.txt` file (which is likely owned by the host user with restrictive permissions), allowing the process to read it and start successfully.

## 2. Verify Fix
I will run the test runner `python3 run_tests.py` again.
-   It should pick up where it left off (`postgresql/ha`).
-   If successful, it will mark it as passed and continue to test the remaining services (`prometheus/standalone`, `rabbitmq/cluster`, etc.).
