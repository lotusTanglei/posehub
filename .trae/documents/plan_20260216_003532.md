# Fix Nacos ARM64 Compatibility Issue

I will resolve the `nacos/standalone` failure caused by the missing ARM64 manifest in the `v2.3.0` image.

## 1. Update `docker-compose.yml`
I will modify `nacos/standalone/docker-compose.yml` to use a newer, multi-arch compatible image tag.
-   **Change**: Update `image: nacos/nacos-server:v2.3.0` to `image: nacos/nacos-server:v2.3.2`.
-   **Reason**: Version `v2.3.2` is known to support ARM64 architecture (Apple Silicon), whereas `v2.3.0` had manifest issues on this platform.

## 2. Verify Fix
I will run the test runner `python3 run_tests.py`.
-   It should pick up where it left off (`nacos/standalone`).
-   It will attempt to pull the new image and start the container.
-   If successful, it will mark `nacos/standalone` as passed and continue to the next services.
