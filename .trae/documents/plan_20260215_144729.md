# Resolve PostgreSQL Port Conflict

I will help you resolve the "port 5432 already allocated" error by ensuring a clean state before testing.

## 1. Modify `validate_runtime.sh` for Better Cleanup
I will enhance the validation script to:
-   **Force Cleanup**: Add a pre-flight check to stop any existing containers using the target ports before starting a new test.
-   **Trap Signals**: Ensure that if the script is interrupted (Ctrl+C), it still attempts to tear down the current service.

## 2. Manual Cleanup Instructions (Since I cannot run docker)
I will provide you with the exact commands to run in your terminal to clear the current conflict:
-   `docker rm -f $(docker ps -aq)` (Nuclear option for dev env) OR
-   `lsof -i :5432` to find the process.

## 3. Update Script Logic
I will modify `validate_runtime.sh` to include a `cleanup` function that runs on exit/interrupt.

## 4. Verify Fix
You will run the updated script.
